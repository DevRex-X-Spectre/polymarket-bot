# Scripts

Operator and development helpers.

Research and trading runtimes must not gain live-order capability from scripts in this directory.

| Script | Purpose |
| --- | --- |
| `bootstrap-research.ps1` | Create the Python 3.12 research venv and install dependencies |
| `test-research.ps1` | Run research pytest using the local venv when present |
